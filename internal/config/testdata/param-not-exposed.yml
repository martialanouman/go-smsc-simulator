# Invalid: the healthy profile exposes no params knobs, so success_rate is not
# exposed → ErrParamNotExposed.
virtual_smscs:
  - name: carrier-a
    port: 2775
    bind_credentials: { system_id: smppclient1, password: secret }
    addr_ton: 1
    addr_npi: 1
    seed: 42
    pdu_buffer_size: 10000
    scenario:
      profile: healthy
      params:
        success_rate: 0.8
      latency:
        distribution: fixed
        params: { ms: 20 }
